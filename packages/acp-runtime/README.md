# ACP runtime package

`@agent-arena/acp-runtime` 提供 ACP stdio 进程监管、协议协商、Session new/resume、配置选择、permission
allowlist、流式 Prompt、delivery ledger 与有界关闭。调用方提供 launch spec、client identity、MCP
servers 与允许的工具集合。
