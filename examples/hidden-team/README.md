# Hidden Team conformance game

Hidden Team 使用两支队伍的私有关键词、公开提示和同步猜测验证 group audience、文字 action、轮换
actor 与 barrier。其 full-stack conformance 同时覆盖 Prompt composition、MatchOrchestrator、密封动作、
transient failure、restart 和 simulation 双 runner。游戏只用于 Core conformance，不提供产品规则或 UI。
